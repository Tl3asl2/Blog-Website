<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= startingContent %> </p>

<% posts.forEach(function(letter){ %>
    <h1><%= (letter.title) %></h1>
    <p><%= (letter.body) %></p>
    <%}); %>

<%- include('partials/footer'); -%>